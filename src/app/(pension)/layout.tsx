import PensionLayout from '@/components/layout/PensionLayout';

export default function PensionRouteLayout({ children }: { children: React.ReactNode }) {
  return <PensionLayout>{children}</PensionLayout>;
}
